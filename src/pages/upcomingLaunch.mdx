---
title: Mission details
---

import Layout from "../components/layout.js"
import { graphql, Link } from "gatsby"
import { Typography, Paper, Box } from "@material-ui/core"
import { Grid } from "@material-ui/core"
import BackButton from "../components/backButton.js"

<Layout>
  <Grid container>
    <Grid item={true} sm={2} xs={1} />
    <Grid item={true} sm={8} xs={10}>
      <Typography gutterBottom variant="h2" />
      <Typography gutterBottom variant="h3">
        {props.pageContext.frontmatter.title.toUpperCase()}
      </Typography>
      <Typography gutterBottom variant="body1">
        {props.data.space_gatsby.launchNext.details}
      </Typography>
      <Typography variant="caption">Note:</Typography>
      <Typography gutterBottom variant="body1">
        This is .mdx file with basic information about next flight.
        <br />
        More info about this mission you can find at{" "}
        <a
          target="_blank"
          rel="noopener noreferrer"
          href={props.data.space_gatsby.launchNext.links.reddit_campaign}
        >
          Reddit campaign
        </a>
      </Typography>
      <BackButton>
        <Link to="/">Back</Link>
      </BackButton>
      <Typography gutterBottom variant="h2" />
    </Grid>
    <Grid item={true} sm={2} xs={1} />
  </Grid>
</Layout>

export const pageQuery = graphql`
  query {
    space_gatsby {
      launchNext {
        details
        links {
          reddit_campaign
        }
      }
    }
  }
`
